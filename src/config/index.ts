const config = {
  PORT: 3000,

  DB_TYPE: 'mysql',
  DB_HOST: 'localhost',
  DB_PORT: 3306,
  DB_USERNAME: 'root',
  DB_PASSWORD: 'hackagon',
  DB_DATABASE: 'meta_sso' // meta_social, meta_chat
}

export default config;